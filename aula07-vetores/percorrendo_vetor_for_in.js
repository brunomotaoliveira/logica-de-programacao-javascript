let vetor = [10, 20, 30, 40, 50, 60, 70, 80, 90];
// percorre o indice do vetor, usado geralmente em vetores do mesmo tamamnho. Permitindo acessar o elemento, de ambos os vetores.
for(let indice in vetor) {
    console.log(vetor[indice]);
}